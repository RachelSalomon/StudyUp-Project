# MongoDB connection string (development)
MONGO_URI=mongodb://127.0.0.1:27017/studyup

# Server port
PORT=5000

# JWT secret (use a long, random string in production)
JWT_SECRET=your_long_random_secret
